<li class="nav-item">
    <a href="{{ path('admin_post_new') }}" class="nav-link">
        <i class="fa fa-plus fa-fw"></i> {{ 'header.new.post'|trans }}
    </a>
</li>

<li class="nav-item">
    <a href="{{ path('admin_tour_new')}}" class="nav-link">
        <i class="fa fa-plus fa-fw"></i> {{ 'header.new.tour'|trans }}
    </a>
</li>

<hr class="sidebar-divider d-none d-md-block">

<li class="searchable nav-item">
    <a href="#" class="nav-link collapsed" data-toggle="collapse" data-target="#collapse-1">
        <i class="fa fa-images fa-fw"></i> {{ 'header.posts'|trans }}<span class="fa arrow"></span>
    </a>
    <ul id="collapse-1" class="collapse list-unstyled">
        {% for post in posts %}
            <li class="searchable nav-item">
                <a href="{{ path('admin_post_edit', {'id': post.id}) }}" class="nav-link p-2">{{ post.name }}</a>
            </li>
        {% endfor %}
    </ul>
</li>
<li class="searchable nav-item">
    <a href="#" class="nav-link collapsed" data-toggle="collapse" data-target="#collapse-2">
        <i class="fa fa-users fa-fw"></i> {{ 'header.authors'|trans }}<span class="fa arrow"></span>
    </a>
    <ul id="collapse-2" class="collapse list-unstyled">
        {% for user in users %}
            <li class="searchable nav-item">
                <a href="{{ path('admin_user_edit', {'id': user.id}) }}" class="nav-link p-2">{{ user }}</a>
            </li>
        {% endfor %}
    </ul>
</li>
<li class="searchable nav-item">
    <a href="#" class="nav-link collapsed" data-toggle="collapse" data-target="#collapse-3">
        <i class="fa fa-globe fa-fw"></i> {{ 'header.locations'|trans }}<span class="fa arrow"></span>
    </a>
    <ul id="collapse-3" class="collapse list-unstyled">
        {% for location in locations %}
            <li class="searchable nav-item">
                <a href="{{ path('admin_location_edit', {'id': location.id}) }}" class="nav-link p-2">{{ location.name }}</a>
            </li>
        {% endfor %}
    </ul>
</li>
<li class="searchable nav-item">
    <a href="#" class="nav-link collapsed" data-toggle="collapse" data-target="#collapse-4">
        <i class="fa fa-hashtag fa-fw"></i> {{ 'header.tags'|trans }}<span class="fa arrow"></span>
    </a>
    <ul id="collapse-4" class="collapse list-unstyled">
        {% for tag in tags %}
            <li class="searchable nav-item">
                <a href="{{ path('admin_tag_edit', {'id': tag.id}) }}" class="nav-link p-2">{{ tag.name }}</a>
            </li>
        {% endfor %}
    </ul>
</li>

<hr class="sidebar-divider d-none d-md-block">

<li class="searchable nav-item">
    <a href="#" class="nav-link collapsed" data-toggle="collapse" data-target="#collapse-5">
        <i class="fa fa-map fa-fw"></i> {{ 'header.tours'|trans }}<span class="fa arrow"></span>
    </a>
    <ul id="collapse-5" class="collapse list-unstyled">
        {% for tour in tours %}
            <li class="searchable nav-item">
                <a href="{{ path('admin_tour_edit', {'id': tour.id}) }}" class="nav-link p-2">{{ tour.name }}</a>
            </li>
        {% endfor %}
    </ul>
</li>
<li class="searchable nav-item">
    <a href="#" class="nav-link collapsed" data-toggle="collapse" data-target="#collapse-6">
        <i class="fa fa-list fa-fw"></i> {{ 'header.tour_categories'|trans }}<span class="fa arrow"></span>
    </a>
    <ul id="collapse-6" class="collapse list-unstyled">
        {% for category in tourCategories %}
            <li class="searchable nav-item">
                <a href="{{ path('admin_tour_category_edit', {'id': category.id}) }}" class="nav-link p-2">{{ category.name }}</a>
            </li>
        {% endfor %}
    </ul>
</li>
