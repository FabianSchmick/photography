twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null
    form_themes:
        - 'templates/form/form.html.twig'
    globals:
        locales: '%app.locales%'
        ga: '%env(string:GOOGLE_ANALYTICS)%'
