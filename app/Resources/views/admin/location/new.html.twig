{% extends 'admin.html.twig' %}

{% block title %}{{ 'new.location'|trans }}{% endblock %}

{% block body %}

<div class="row">
    <div class="col-xs-12">
        <h1 class="page-header">{{ 'new.location'|trans }}</h1>

        <form id="newLocation" class="form-horizontal" action="{{ path('location_new') }}" method="post">
            <div class="form-group">
                <label class="control-label col-sm-2" for="name">{{ 'name'|trans }}*</label>
                <div class="col-sm-10">
                    <input id="name" class="form-control" type="text" name="new[name]" required="required"/>
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button class="btn btn-success" type="submit" name="submit">{{ 'submit'|trans }}</button>
                </div>
            </div>
        </form>
    </div>
</div>

{% endblock %}
